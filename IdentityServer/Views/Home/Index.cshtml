@page "/"

@{
    Layout = null;

    // IdentityServer.Program.logSink.Lines.CollectionChanged += (s, e) =>
    // {
    //     //@Html.EditorFor(m => IdentityServer.Program.logSink.Lines, "Lines")
        
    // };

}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>IdentityServer4</title>
    <style>
        * {
            font-family: 'Consolas', sans-serif;
            box-sizing: border-box;
        }
        body {
            margin: 0;
        }
        .container {
            background-color: black;
            display: flex;
            flex-direction: column;
            align-items: self-start;
            gap: 10px;
            min-height: 100vh;
            padding: 0.5em;
        }
    </style>
</head>
<body>
    <div><a href="/swagger">Swagger</a></div>
    <span class="container" id="logcontainer">
    </span>


    <script type="text/javascript" src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script type="text/javascript">
        console.debug('LOAD');
        $(document).ready(function () {
            var url = '@Url.Action("FetchLogs")';
            var containerElement = $("#logcontainer");
            setInterval(function () {
                console.debug('LOADING ' + url);
                containerElement.load(url);
            }
                , 2000);
        });
    </script>
</body>

</html>



<!-- await Html.PartialAsync("LogItemsList", IdentityServer.Program.logSink.Lines) -->